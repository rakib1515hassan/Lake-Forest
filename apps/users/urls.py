from django.urls import path
from apps.users import views

app_name = 'users'

urlpatterns = [
    # path('login/', views.LoginView.as_view(), name='login'),
    # path('logout/', views.LogoutView.as_view(), name='logout'),
    # path('restrict/', views.RestrictedView.as_view(), name='restrict'),

    # path('user-create/', views.UserCreateView.as_view(), name='user-create'),
    # path('user-list/', views.UserListView.as_view(), name='user-list'),
    # path('user/<uuid:pk>/details/', views.UserDetailsView.as_view(), name='user-details'),
    # path('user/<uuid:pk>/update/', views.UserUpdateView.as_view(), name='user-update'),
    # path('user/<uuid:user_id>/change-password/', views.UserChangePasswordView.as_view(), name='change-password'),
    # path('user/<uuid:user_id>/permission-update/', views.UserPermissionUpdateView.as_view(), name='permission-update'),
    # path('user/<uuid:user_id>/user-delete/', views.UserDeleteView.as_view(), name='user-delete'),
    
]